# sign-in-with-stacks + better-auth Example

An example of sign-in-with-stacks and better-auth.

## Getting Started

First, run the development server:

```bash
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser and login with your wallet.
